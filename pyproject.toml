[project]
name = "romsection"
version = "1.0.0"

requires-python = ">= 3.10"

dependencies = [
  "rtoml",
  "pyqt5",
  "numpy",
  "silx",
  "lru-dict",
]

[project.optional-dependencies]
dev = [
    "black",
    "pytest",
    "mypy",
    "PyQt5-stubs",
]
